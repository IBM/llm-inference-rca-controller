---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: resourceclaimautoscalers.autoscaling.x-llmd.ai
spec:
  group: autoscaling.x-llmd.ai
  names:
    kind: ResourceClaimAutoscaler
    listKind: ResourceClaimAutoscalerList
    plural: resourceclaimautoscalers
    shortNames:
    - rca
    singular: resourceclaimautoscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.target.serviceRef.name
      name: Service
      type: string
    - jsonPath: .status.scalingStatus.desiredReplicas
      name: Desired
      type: integer
    - jsonPath: .status.scalingStatus.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.scalingStatus.availableReplicas
      name: Available
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceClaimAutoscaler is the Schema for the resourceclaimautoscalers
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourceClaimAutoscalerSpec defines the desired state of
              ResourceClaimAutoscaler.
            properties:
              behavior:
                description: Behavior defines the scaling behaviors
                properties:
                  scaleDown:
                    description: ScaleDown defines the behavior for scaling down
                    properties:
                      gracePeriodSeconds:
                        description: GracePeriodSeconds is the grace period before
                          patching
                        format: int32
                        minimum: 0
                        type: integer
                      singleTimeScalingLimit:
                        description: SingleTimeScalingLimit limits the amount of resource
                          that can be scaled at a single time
                        format: int32
                        minimum: 1
                        type: integer
                      trigger:
                        description: Trigger defines when to trigger scaling
                        properties:
                          periodSeconds:
                            description: |-
                              PeriodSeconds is the static monitoring period in seconds
                              This is the interval at which metrics are collected
                            format: int32
                            minimum: 1
                            type: integer
                        required:
                        - periodSeconds
                        type: object
                    required:
                    - gracePeriodSeconds
                    - trigger
                    type: object
                  scaleUp:
                    description: ScaleUp defines the behavior for scaling up
                    properties:
                      gracePeriodSeconds:
                        description: GracePeriodSeconds is the grace period before
                          patching
                        format: int32
                        minimum: 0
                        type: integer
                      singleTimeScalingLimit:
                        description: SingleTimeScalingLimit limits the amount of resource
                          that can be scaled at a single time
                        format: int32
                        minimum: 1
                        type: integer
                      trigger:
                        description: Trigger defines when to trigger scaling
                        properties:
                          periodSeconds:
                            description: |-
                              PeriodSeconds is the static monitoring period in seconds
                              This is the interval at which metrics are collected
                            format: int32
                            minimum: 1
                            type: integer
                        required:
                        - periodSeconds
                        type: object
                    required:
                    - gracePeriodSeconds
                    - trigger
                    type: object
                type: object
              constraints:
                description: Constraints defines the scaling constraints
                properties:
                  capacityPerCount:
                    additionalProperties:
                      description: CapacityPerCountConstraint defines min, max, and
                        step capacity constraints per count
                      properties:
                        max:
                          anyOf:
                          - type: integer
                          - type: string
                          description: Max is the maximum capacity per count
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        min:
                          anyOf:
                          - type: integer
                          - type: string
                          description: Min is the minimum capacity per count
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        step:
                          anyOf:
                          - type: integer
                          - type: string
                          description: Step is the increment step for capacity changes
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                      type: object
                    description: |-
                      CapacityPerCount is a map between capacity name and its min/max/step constraints per count
                      Used to specify minimum, maximum, and step resource allocation per GPU/count
                      Example: {"compute": {min: "1", max: "100", step: "1"}, "memory": {min: "8Gi", max: "80Gi", step: "1Gi"}}
                    type: object
                  maxCountPerReplica:
                    description: MaxCountPerReplica is the maximum count per replica
                    format: int32
                    minimum: 1
                    type: integer
                  maxReplicas:
                    description: MaxReplicas is the maximum number of replicas
                    format: int32
                    minimum: 1
                    type: integer
                  minCountPerReplica:
                    default: 1
                    description: MinCountPerReplica is the minimum count per replica.
                      Default is 1, must be positive
                    format: int32
                    minimum: 1
                    type: integer
                  minReplicas:
                    default: 1
                    description: MinReplicas is the minimum number of replicas. Default
                      is 1, allows 0 for scale to zero
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              minTargetUtilization:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: |-
                  MinTargetUtilization defines the minimum target utilization per resource type
                  Maps resource type (e.g., "nvidia.com/gpu", "memory") to the minimum desired utilization ratio
                  When (usage / allocation) falls below this threshold, scale-down may be triggered
                  Values should be between 0.0 and 1.0 representing utilization percentage
                  Example: {"nvidia.com/gpu": Quantity("0.7")} means target 70% GPU utilization
                type: object
              target:
                description: Target defines the scaling target selector
                properties:
                  hint:
                    description: Hint provides initial prediction hints for prefill/decode
                      latency
                    properties:
                      PrefixCacheHitRate:
                        default: 0.8
                        description: PrefixCacheHitRate is the threshold for KV cache
                          hits (0.0 to 1.0)
                        type: number
                      batchSize:
                        default: 1
                        description: |-
                          BatchSize is the batch size for inference (default: 1)
                          This is different from MaxNumSeqs which represents concurrency
                        format: int32
                        minimum: 1
                        type: integer
                      gpuName:
                        description: GPUName
                        type: string
                      kvCacheSpeedup:
                        default: 1
                        description: |-
                          KVCacheSpeedup is the speedup factor when KV cache is hit (0.0 to 1.0)
                          Affects latency calculation
                        minimum: 0
                        type: number
                      maxNumBatchedTokens:
                        description: MaxNumBatchedTokens is the maximum total tokens
                          that can be processed
                        format: int32
                        minimum: 1
                        type: integer
                      maxNumSeq:
                        default: 1
                        description: ' MaxNumSeqs represents concurrency'
                        format: int32
                        minimum: 1
                        type: integer
                      modelID:
                        description: ModelID is the model name
                        type: string
                      modelParams:
                        description: |-
                          ModelParams defines the model architecture parameters
                          If not provided, parameters will be inferred from ModelID
                        properties:
                          hiddenSize:
                            description: HiddenSize is the hidden dimension size
                            minimum: 128
                            type: integer
                          kvHeads:
                            description: KVHeads is the number of key-value attention
                              heads
                            minimum: 1
                            type: integer
                          layers:
                            description: Layers is the number of transformer layers
                            minimum: 1
                            type: integer
                          parametersB:
                            description: ParametersB is the model size in billions
                              of parameters
                            minimum: 0.1
                            type: number
                          precision:
                            default: 2
                            description: Precision is the precision in bytes (1=FP8/INT8,
                              2=FP16/BF16, 4=FP32)
                            maximum: 4
                            minimum: 1
                            type: integer
                          queryHeads:
                            description: QueryHeads is the number of query attention
                              heads
                            minimum: 1
                            type: integer
                        required:
                        - hiddenSize
                        - kvHeads
                        - layers
                        - parametersB
                        - queryHeads
                        type: object
                    type: object
                  resourceRef:
                    description: ResourceRef references the target resource claim
                      to make a new revision
                    properties:
                      deviceClassName:
                        description: |-
                          DeviceClassName is the name of the device class to target
                          This is used to select resource claims of the service owner
                        type: string
                    required:
                    - deviceClassName
                    type: object
                  serviceRef:
                    description: ServiceRef references the target service to determine
                      the pod-template to patch
                    properties:
                      name:
                        description: Name of the service
                        type: string
                      namespace:
                        description: Namespace of the service. Defaults to the namespace
                          of the ResourceClaimAutoscaler
                        type: string
                    required:
                    - name
                    type: object
                required:
                - resourceRef
                - serviceRef
                type: object
              targetLatency:
                description: TargetLatency defines the service latency targets
                properties:
                  endToEndLatencyMilliseconds:
                    description: EndToEndLatencyMilliseconds is the target end-to-end
                      latency in milliseconds
                    format: int32
                    minimum: 0
                    type: integer
                  interTokenLatencyMilliseconds:
                    description: InterTokenLatencyMilliseconds is the target inter-token
                      latency in milliseconds
                    format: int32
                    minimum: 0
                    type: integer
                  timeToFirstTokenLatencyMilliseconds:
                    description: TimeToFirstTokenLatencyMilliseconds is the target
                      time to first token latency in milliseconds
                    format: int32
                    minimum: 0
                    type: integer
                  toleranceMilliseconds:
                    description: ToleranceMilliseconds is the latency violation tolerance
                      in milliseconds
                    format: int32
                    minimum: 0
                    type: integer
                type: object
            required:
            - target
            type: object
          status:
            description: ResourceClaimAutoscalerStatus defines the observed state
              of ResourceClaimAutoscaler.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the autoscaler's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              discovery:
                description: Discovery contains the discovery results
                properties:
                  owner:
                    description: Owner is the detected owner of the service
                    properties:
                      apiVersion:
                        description: API version of the referent.
                        type: string
                      blockOwnerDeletion:
                        description: |-
                          If true, AND if the owner has the "foregroundDeletion" finalizer, then
                          the owner cannot be deleted from the key-value store until this
                          reference is removed.
                          See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
                          for how the garbage collector interacts with this field and enforces the foreground deletion.
                          Defaults to false.
                          To set this field, a user needs "delete" permission of the owner,
                          otherwise 422 (Unprocessable Entity) will be returned.
                        type: boolean
                      controller:
                        description: If true, this reference points to the managing
                          controller.
                        type: boolean
                      kind:
                        description: |-
                          Kind of the referent.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                        type: string
                      name:
                        description: |-
                          Name of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
                        type: string
                      uid:
                        description: |-
                          UID of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
                        type: string
                    required:
                    - apiVersion
                    - kind
                    - name
                    - uid
                    type: object
                    x-kubernetes-map-type: atomic
                  resourceClaim:
                    description: ResourceClaim is the original resource claim
                    type: string
                type: object
              scalingResults:
                description: ScalingResults contains the scaling estimation results
                properties:
                  lastEstimation:
                    description: LastEstimation contains the last latency estimation
                    properties:
                      interTokenLatencyMilliseconds:
                        description: InterTokenLatencyMilliseconds is the estimated
                          inter-token latency
                        format: int32
                        minimum: 0
                        type: integer
                      interTokenLatencyOffsetMilliseconds:
                        description: |-
                          InterTokenLatencyOffsetMilliseconds is the cumulative offset applied to ITL estimation
                          This is the difference between measured and calculated values from previous rounds
                        format: int32
                        type: integer
                      timeToFirstTokenMilliseconds:
                        description: TimeToFirstTokenMilliseconds is the estimated
                          time to first token latency
                        format: int32
                        minimum: 0
                        type: integer
                      timeToFirstTokenOffsetMilliseconds:
                        description: |-
                          TimeToFirstTokenOffsetMilliseconds is the cumulative offset applied to TTFT estimation
                          This is the difference between measured and calculated values from previous rounds
                        format: int32
                        type: integer
                    type: object
                  lastEstimationTime:
                    description: LastEstimationTime is the timestamp of the last estimation
                    format: date-time
                    type: string
                type: object
              scalingStatus:
                description: ScalingStatus contains the current scaling status
                properties:
                  availableReplicas:
                    description: AvailableReplicas is the number of available replicas
                    format: int32
                    type: integer
                  desiredClaimTemplate:
                    description: DesiredClaimTemplate is the desired resource claim
                      template
                    type: string
                  desiredReplicas:
                    description: DesiredReplicas is the number of desired replicas
                    format: int32
                    type: integer
                  lastApplyTime:
                    description: LastApplyTime is the timestamp when the claim was
                      last applied
                    format: date-time
                    type: string
                  oldestClaimTemplate:
                    description: OldestClaimTemplate is the oldest resource claim
                      template still in use
                    type: string
                  readyReplicas:
                    description: ReadyReplicas is the number of ready replicas
                    format: int32
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
