---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: rcacontrollerconfigs.autoscaling.x-llmd.ai
spec:
  group: autoscaling.x-llmd.ai
  names:
    kind: RCAControllerConfig
    listKind: RCAControllerConfigList
    plural: rcacontrollerconfigs
    singular: rcacontrollerconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RCAControllerConfig is the Schema for the rcacontrollerconfigs
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RCAControllerConfigSpec defines the desired state of RCAControllerConfig.
            properties:
              logging:
                description: Logging defines the logging configuration
                properties:
                  level:
                    default: INFO
                    description: Level is the logging level
                    enum:
                    - DEBUG
                    - INFO
                    - WARN
                    - ERROR
                    type: string
                type: object
              monitoring:
                description: Monitoring defines the monitoring configuration
                properties:
                  customQueries:
                    description: CustomQueries defines custom Prometheus queries
                    properties:
                      deviceResourceMetrics:
                        additionalProperties:
                          description: DeviceResourceMetrics defines usage and allocation
                            queries for a device resource type
                          properties:
                            allocationQuery:
                              description: AllocationQuery is the Prometheus query
                                for resource allocation limit
                              type: string
                            usageQuery:
                              description: UsageQuery is the Prometheus query for
                                current resource usage
                              type: string
                          type: object
                        description: |-
                          DeviceResourceMetrics maps resource type names (e.g., "compute", "memory") to their usage/allocation queries
                          Example: {"compute": {usageQuery: "vllm:gpu_active_thread_percentage", allocationQuery: "vllm:gpu_compute_allocation_percentage"}}
                        type: object
                      endToEndLatencyBucketQuery:
                        default: vllm:e2e_request_latency_seconds_bucket
                        description: EndToEndLatencyBucketQuery is the Prometheus
                          query for end-to-end latency buckets
                        type: string
                      generationTokenQuery:
                        default: vllm:generation_tokens_total
                        description: GenerationTokenQuery is the Prometheus query
                          for generation tokens
                        type: string
                      interTokenLatencyQuery:
                        default: vllm:inter_token_latency_seconds
                        description: InterTokenLatencyQuery is the Prometheus query
                          for inter-token latency
                        type: string
                      prefillTokensQuery:
                        default: vllm:prefill_tokens_total
                        description: PrefillTokensQuery is the Prometheus query for
                          prefill tokens
                        type: string
                      prefixCacheHitsQuery:
                        default: vllm:prefix_cache_hits
                        description: PrefixCacheHitsQuery is the Prometheus query
                          for vLLM prefix cache hits
                        type: string
                      prefixCacheQueriesQuery:
                        default: vllm:prefix_cache_queries
                        description: PrefixCacheQueriesQuery is the Prometheus query
                          for vLLM prefix cache queries
                        type: string
                      promptTokenQuery:
                        default: vllm:prompt_tokens_total
                        description: PromptTokenQuery is the Prometheus query for
                          prompt tokens
                        type: string
                      queueTimeQuery:
                        default: vllm:request_queue_time_seconds_bucket
                        description: QueueTimeQuery is the Prometheus query for request
                          queue time
                        type: string
                      rpsQuery:
                        default: vllm:request_success_total
                        description: RPSQuery is the Prometheus query for requests
                          per second
                        type: string
                      timeToFirstTokenLatencyQuery:
                        default: vllm:time_to_first_token_seconds
                        description: TimeToFirstTokenLatencyQuery is the Prometheus
                          query for time to first token latency
                        type: string
                    type: object
                  endpoint:
                    description: Endpoint is the Prometheus monitoring endpoint
                    type: string
                  latencyPercentile:
                    default: 0.95
                    description: LatencyPercentile is the percentile for latency calculations
                      (0.0 to 1.0)
                    maximum: 1
                    minimum: 0
                    type: number
                  rateRangeSeconds:
                    description: RateRangeSeconds is the time range in seconds for
                      rate calculations
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - endpoint
                type: object
            required:
            - monitoring
            type: object
          status:
            description: RCAControllerConfigStatus defines the observed state of RCAControllerConfig.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the config's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              monitoringStatus:
                description: MonitoringStatus contains the monitoring connection and
                  validation status
                properties:
                  connected:
                    description: Connected indicates whether the monitoring endpoint
                      is reachable
                    type: boolean
                  message:
                    description: Message provides additional information about the
                      connection status
                    type: string
                  metricHealths:
                    description: MetricHealths contains the health status of each
                      monitored metric
                    items:
                      description: MetricHealth defines the health status of a specific
                        metric
                      properties:
                        healthy:
                          description: Healthy indicates whether the metric is available
                            and valid
                          type: boolean
                        message:
                          description: Message provides additional information about
                            the metric health
                          type: string
                        name:
                          description: Name is the name of the metric
                          type: string
                        query:
                          description: Query is the Prometheus query used to derive
                            a metric
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
